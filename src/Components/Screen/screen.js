import React from "react";

const Screen = ({text}) => {
    const screenOneStyles = {
        background : "url"
    }
    return (
        <div>
            <h1 style={{fontFamily : 'GhotamPro'}}>{text}</h1>
        </div>
    )
}
export default Screen;